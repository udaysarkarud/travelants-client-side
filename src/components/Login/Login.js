import React from 'react';

const Login = () => {
    return (
        <section className="container">
            <h2>Login / Singup</h2>
            <button>Login</button>
        </section>
    );
};

export default Login;